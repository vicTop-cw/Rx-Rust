[package]
name = "rxpy"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
name = "rxpy"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rx_rust = { path = "../rx-rust" }
